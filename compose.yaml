services:
  postgres:
    image: 'postgres:latest'
    restart: unless-stopped
    environment:
      - 'POSTGRES_DB=andres_stream_db'
      - 'POSTGRES_PASSWORD=andresian1234'
      - 'POSTGRES_USER=andresian'
    ports:
      - '5432'
    volumes:
      - pgdata:/data/postgres
volumes:
  pgdata:
